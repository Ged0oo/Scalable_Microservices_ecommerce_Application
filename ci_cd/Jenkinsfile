pipeline {
  agent any
  stages {
    stage('Deploy via Ansible') {
      steps {
        sh '''
          ansible-playbook -i infra/ansible/hosts.ini infra/ansible/deploy_app.yml --extra-vars "docker_hub_password=${DOCKER_HUB_PASSWORD}"
        '''
      }
    }
  }
}
